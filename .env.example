# Optional: set a shared token for webhook and consumer access
WEBHOOK_TOKEN=

# Optional: bind host for LAN/public usage
HOST=0.0.0.0

# Optional: port override
PORT=8080

# Optional: forward each processed message to another service
FORWARD_WEBHOOK_URL=
